# GoGoG Backend Environment Configuration

# Server Settings
PORT=8080
ENV=development  # development, production

# Database Settings
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=gogog
DB_SSLMODE=disable

# Authentication
JWT_SECRET=change_this_to_a_secure_random_string